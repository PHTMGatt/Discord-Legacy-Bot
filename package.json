{
  "name": "discord-local-bot",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "echo 'no build step needed'",
    "render-build": "npm install && npm run build"
  },
  "dependencies": {
    "discord-local-bot": "file:",
    "discord.js": "^14.11.0",
    "dotenv": "^16.3.1",
    "express": "^4.21.2"
  }
}
